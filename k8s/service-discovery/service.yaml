apiVersion: v1
kind: Service
metadata:
  name: service-discovery
  namespace: microservices
spec:
  type: ClusterIP  # ← Cambiar aquí
  selector:
    app: service-discovery
  ports:
    - port: 8761
      targetPort: 8761